import { createContext } from 'react';

// Provide a default value for MyContext
export const MyContext = createContext(null); // or any appropriate default value

// Provide a default value for MySecondContext
export const MySecondContext = createContext(null); // or any appropriate default value
